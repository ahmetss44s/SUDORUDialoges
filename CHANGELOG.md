# Changelog

## [v1.1.3] — 2026-03-21

### 💰 Смена валюты: Scoreboard Coins (Breaking Change)
- **Убрана item-валюта** (изумруды + шалкеры) из всей логики покупки.
- Теперь валюта — Scoreboard-объект `Coins` (тип `dummy`), создаваемый датапаком.
- `getCurrencyAmount()` и `takeCurrency()` обращаются к `Bukkit.getScoreboardManager().getMainScoreboard()`.
- Если датапак не загружен и `Coins` objective не существует — плагин логирует предупреждение.
- `config.yml`: секция `currency.type/item-material` закомментирована; осталась только `currency.name: "Coins"`.

### ✨ Новое: `/sellshop` — диалог продажи
- **`SellShopDialog`** (Paper Dialog API) — аналог покупки, но для продажи.
  - Сканирует инвентарь на предметы с PDC-ключом `sudoru:shop_price`.
  - Показывает список с ценой: `[Название ×кол-во → +Coins] [Продать]`.
  - При продаже: убирает стак из инвентаря, начисляет `amount × pricePerItem` Coins.
- **`SellShopCommand`** `/sellshop [open]` — пермишн `sudoru.sellshop` (default: true).
  - Вызывается напрямую или из датапака: `execute as @p run sellshop open`.
- **`TraderShop.buildRewardItem()`** теперь тегирует купленные предметы PDC `sudoru:shop_price` (Coins за 1 шт.).

### 🗑️ Удалено
- `getCurrencyMaterial()`, `isShulkerBox()`, `getAmountFromShulker()`, `takeFromShulker()` из `SUDORUDialoges.java`.

---

## [v1.0.8] — 2026-03-15

### ✨ Новое
- **Карточка товара** — добавлена отдельная строка с **жирным названием предмета** над иконкой.
  Теперь структура карточки: `Название` → `Иконка + описание` → `[Купить ×1] [Купить ×?]`.

---

## [v1.0.7] — 2026-03-15

### 📌 Правило версионирования
- Каждое изменение/добавление функций теперь сопровождается поднятием версии.

### ✨ Новое (перенесено из v1.0.6)
- **Кнопка «Купить ×?»** — вводишь количество в чат, плагин суммирует итоговую стоимость
  (`кол-во × цена`), проверяет наличие валюты и проводит покупку.
- **Жирные названия предметов** — во всех интерфейсах (инвентарь и Paper Dialog).
- **Весь интерфейс на русском** — убрана транслитерация.

### 🐛 Исправления (перенесено из v1.0.6)
- **Валюта** — `getContents()` возвращает копию массива; теперь каждый слот явно
  записывается через `inv.setItem(i, null/stack)` — валюта списывается корректно.
- **Кастомный торговец** — `TraderManager` больше не пропускает торговцев с пустым
  списком предметов; меню редактора и добавление предметов работают корректно.
- **Торговец не удаляется** из системы при пустом списке предметов.
- **Индексы предметов** — отображаются с `#1` вместо `#0`.

---

## [v1.0.5] — 2026-03-15

### ✨ Новый дизайн инвентаря торговца

- **Карточки предметов** — каждый товар отображается вертикальной карточкой:
  - Строка 1: название предмета (бумага с ценой в lore)
  - Строка 2: иконка предмета (сам предмет с полным описанием)
  - Строка 3: кнопка **Купить ×1** (зелёная)
  - Строка 4: кнопка **Купить ×5** (синяя, покупает 5 комплектов)
- **Панель описания** справа (колонки 6-8):
  - Иконка торговца
  - Описание торговца (разбито по строкам)
  - Информация об обновлении ассортимента

---

## [v1.0.4] — 2026-03-15

### 🐛 Исправления

- **TraderDialogMenu:** кнопка «Выход» теперь корректно закрывает диалог (`closeInventory` на main thread)
- **TraderShop:** при покупке игрок получает **чистый предмет** — без лора магазина (цены, "Нажми кнопку ниже" и т.д.)

---

## [v1.0.3] — 2026-03-15

### 🔧 Исправления

- **TraderDialogMenu:** удалён неиспользуемый импорт `DialogAction`
- **TraderDialogMenu:** добавлена аннотация `@SuppressWarnings("UnstableApiUsage")` — подавление предупреждений нестабильного Paper Dialog API

### ✨ Добавлено

- Файл `CHANGELOG.md` для отслеживания истории изменений

---

## [v1.0.2] — 2026-03-15 — Последний стабильный релиз

### 🔧 Исправления

- **Валюта:** исправлено снятие предметов-валюты из инвентаря игрока (`EventPriority.HIGHEST` + `updateInventory`)
- **HEX цвета:** исправлена обработка цветов `&#RRGGBB` в названиях (`Matcher.quoteReplacement` + `toUpperCase`)
- **TraderMenu:** исправлено отображение «предметов» в `/tradermenu` при открытии редактора (вкладка `/traderconfig items`)
- **NPE:** исправлен `NullPointerException` при обращении к пустым слотам/предметам

### ✨ Добавлено

- Автоматически создаются **3 встроенных торговца** с 5+ предметами при первом запуске плагина
- Новые предметы в пулах (слоты 5–6 видов на каждого торговца)
- Обновлён **README**: подробное описание команд, прав и настроек

---

## [v1.0.1] — первый релиз

- Начальная версия плагина
- Нативное диалоговое меню (Paper Dialog API 1.21.5+)
- GUI-редактор торговцев (`/tradermenu`, `/traderconfig`)
- Поддержка PlaceholderAPI
- Предметная валюта (изумруды по умолчанию)
